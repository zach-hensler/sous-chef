services:
  sous-chef-tests-db:
    image: 'postgres:latest'
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: test-user
      POSTGRES_PASSWORD: test-pass
      POSTGRES_DB: postgres
